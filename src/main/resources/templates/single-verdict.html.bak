<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title th:text="${title}">Is $100k good in Austin?</title>

    <link rel="stylesheet" href="/css/tokens.css">
    <link rel="stylesheet" href="/css/core.css">
    <link rel="stylesheet" href="/css/result.css">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=JetBrains+Mono:wght@500;700&display=swap"
        rel="stylesheet">

    <!-- SEO: Canonical Tag -->
    <link rel="canonical" th:href="${canonicalUrl}">
    <meta name="description" th:content="${metaDescription}">

    <!-- JSON-LD Structured Data - DECISION GAP FRAMEWORK (Phase 3) -->
    <!-- Principle: FAQPage with conditional answers - NO exact salary numbers -->
    <!-- Prevents Zero-Click while maintaining rich results eligibility -->
    <script type="application/ld+json" th:inline="javascript">
    {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": [
            {
                "@type": "Question",
                "name": /*[['Is $' + ${salary} + ' a good salary in ' + ${city.city} + '?']]*/ "Is this salary good in this city?",
                "acceptedAnswer": {
                    "@type": "Answer",
                    "text": /*[['It depends on your lifestyle and family situation. In ' + ${city.city} + ', you need to consider ' + ${city.state} + ' state taxes, housing costs (avg. rent ~$' + ${city.avgRent} + '/mo), and your personal spending. Use our verdict calculator to see your real residual income.']]*/ "It depends on your lifestyle..."
                }
            },
            {
                "@type": "Question",
                "name": /*[['What is the cost of living in ' + ${city.city} + '?']]*/ "What is the cost of living?",
                "acceptedAnswer": {
                    "@type": "Answer",
                    "text": /*[['The primary cost factors in ' + ${city.city} + ' are housing and taxes. ' + ${city.state} + ' has specific tax rates. Your actual cost depends on neighborhood choice, lifestyle, and whether you own or rent. Our calculator breaks down all major expense categories.']]*/ "The primary factors are housing and taxes..."
                }
            },
            {
                "@type": "Question",
                "name": "How accurate is this salary analysis?",
                "acceptedAnswer": {
                    "@type": "Answer",
                    "text": "We use IRS 2025 tax tables, BLS wage data, and Numbeo cost-of-living indices. Results are high-fidelity estimates based on statistical averages. Your personal outcome depends on lifestyle choices, debt obligations, and housing preferences. This is a strategic baseline for decision-making, not financial advice. Consult a CFP for personalized guidance."
                }
            }
        ]
    }
    </script>
    <!-- Analytics -->
    <div th:replace="~{fragments/analytics :: analytics}"></div>
</head>

<body class="theme-lucid">

    <nav class="top-nav">
        <div class="container">
            <a href="/" class="logo">OfferVerdict</a>
        </div>
    </nav>

    <main class="main-content">
        <!-- LAYER 1: VERDICT -->
        <section class="verdict-section"
            th:classappend="${verdict == T(com.offerverdict.model.Verdict).GO ? 'premium-gold' : (verdict == T(com.offerverdict.model.Verdict).NO_GO ? 'harsh-red' : 'neutral-blue')}">
            <div class="container">
                <!-- BREADCRUMBS -->
                <nav aria-label="Breadcrumb" class="breadcrumb-nav"
                    style="margin-bottom: 1.5rem; font-size: 0.9rem; color: #666;">
                    <a href="/"
                        style="color: inherit; text-decoration: none; border-bottom: 1px solid transparent;">Home</a>
                    <span style="margin: 0 0.5rem;">&rsaquo;</span>
                    <span th:text="${city.state} + ' Cities'">State Cities</span>
                    <span style="margin: 0 0.5rem;">&rsaquo;</span>
                    <span th:text="${city.city} + ' Salary Analysis'" style="font-weight: 600; color: #333;">City
                        Analysis</span>
                </nav>

                <div class="verdict-header">
                    <span class="badge-source">SINGLE CITY ANALYSIS</span>
                </div>

                <div class="verdict-display">
                    <h1 class="verdict-text animate-pop" th:text="${verdict}">GO</h1>
                    <div class="verdict-sub" th:text="${verdictMsg}">This salary offers a strong lifestyle.</div>

                    <div class="leverage-card">
                        <div class="leverage-content">
                            <p class="leverage-msg" th:text="${introText}">Congratulations! This is a powerful income.
                            </p>
                        </div>
                    </div>
                </div>

                <div class="financial-impact">
                    <div class="impact-label">Estimated Monthly Residual (Savings)</div>
                    <div class="impact-value"
                        th:text="${'$' + #numbers.formatInteger(result.residual, 0, 'COMMA') + '/mo'}">$2,500/mo</div>
                    <div class="contextual-disclaimer"
                        style="margin-top: 12px; font-size: 0.85rem; color: #666; font-style: italic;">
                        <span th:text="${contextualDisclaimer}">*Figures are estimates based on public data.</span>
                    </div>
                </div>

                <!-- Ad Slot: Top -->
                <div class="ad-container-top" id="ad-top-verdict">
                    <div class="ad-label">ADVERTISEMENT</div>
                    <div class="ad-placeholder">
                        <span class="ad-hint">Premium Financial Partner Slot</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- LAYER 2: BREAKDOWN -->
        <section class="evidence-section">
            <div class="container">
                <div class="section-title">
                    <h2>Monthly Breakdown</h2>
                    <p
                        th:text="${'Where your $' + #numbers.formatInteger(salary, 0, 'COMMA') + ' actually goes in ' + city.city + '.'}">
                        Where your money goes.</p>
                </div>

                <div class="receipt-grid single-analysis-grid">
                    <div class="receipt-card"
                        th:with="grossMo=${salary/12}, taxMo=${(result.taxResult.totalTax + (result.localTax*12))/12}, gap=${(salary/12) - ((result.taxResult.totalTax + (result.localTax*12))/12) - result.rent - result.livingCost - result.residual}">
                        <div class="card-header">
                            <h3 th:text="${city.city}">Austin</h3>
                            <span class="badge">Stats</span>
                        </div>
                        <div class="line-item">
                            <span class="label">Gross Monthly</span>
                            <span class="value mono"
                                th:text="${'$' + #numbers.formatInteger(grossMo, 0, 'COMMA')}">$8,333</span>
                        </div>
                        <div class="line-item danger-text">
                            <span class="label">Taxes (Est.)</span>
                            <span class="value mono"
                                th:text="${'-$' + #numbers.formatInteger(taxMo, 0, 'COMMA')}">-$2,000</span>
                        </div>
                        <div class="line-item warning-text">
                            <span class="label">Housing (Avg. Rent)</span>
                            <span class="value mono"
                                th:text="${'-$' + #numbers.formatInteger(result.rent, 0, 'COMMA')}">-$1,900</span>
                        </div>
                        <div class="line-item">
                            <span class="label">Living Costs</span>
                            <span class="value mono"
                                th:text="${'-$' + #numbers.formatInteger(result.livingCost, 0, 'COMMA')}">-$1,200</span>
                        </div>
                        <div class="line-item danger-text">
                            <span class="label">Benefits & 401k</span>
                            <span class="value mono"
                                th:text="${'-$' + #numbers.formatInteger(gap, 0, 'COMMA')}">-$610</span>
                        </div>
                        <div class="line-item success-text highlight-row">
                            <span class="label">True Residual</span>
                            <span class="value mono"
                                th:text="${'$' + #numbers.formatInteger(result.residual, 0, 'COMMA')}">$3,233</span>
                        </div>
                    </div>
                </div>

                <div class="analysis-box">
                    <p><strong>Housing Check:</strong> <span th:text="${housingWarning}">Housing costs are
                            reasonable.</span></p>
                    <p><strong>Verdict:</strong> <span th:text="${analysisText}">This is a
                            solid financial move.</span></p>
                </div>

            </div>
        </section>

        <!-- SALARY NEIGHBORS (Internal Linking) -->

        <!-- SEO Enhancement: City Context Section -->
        <section class="city-context-section" th:if="${cityContext != null}">
            <div class="container">
                <div class="section-title">
                    <h2>Understanding <span th:text="${city.city}">City</span></h2>
                    <p>Key insights about living and working in <span th:text="${city.city}">City</span>.</p>
                </div>

                <div class="reality-grid">
                    <div class="reality-card">
                        <div class="icon">🏢</div>
                        <div class="reality-content">
                            <h4>Job Market</h4>
                            <p th:text="${cityContext.jobMarketSummary}">Job market information...</p>
                        </div>
                    </div>

                    <div class="reality-card">
                        <div class="icon">🏘️</div>
                        <div class="reality-content">
                            <h4>Housing Market</h4>
                            <p th:text="${cityContext.housingTrends}">Housing trends...</p>
                        </div>
                    </div>

                    <div class="reality-card">
                        <div class="icon">🏭</div>
                        <div class="reality-content">
                            <h4>Industry Focus</h4>
                            <p th:text="${cityContext.industryFocus}">Industry information...</p>
                        </div>
                    </div>

                    <div class="reality-card">
                        <div class="icon">🌟</div>
                        <div class="reality-content">
                            <h4>Quality of Life</h4>
                            <p th:text="${cityContext.qualityOfLife}">Quality of life...</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- SALARY NEIGHBORS (Internal Linking) -->
        <!-- SALARY NEIGHBORS (Internal Linking) -->
        <section class="neighbors-section" style="padding: 3rem 0; background: #fafafa; border-top: 1px solid #eee;">
            <div class="container">
                <div class="section-title" style="margin-bottom: 2rem; text-align: center;">
                    <h3>Explore Salary Ranges</h3>
                    <p>See how your lifestyle changes with a different income.</p>
                </div>

                <div class="salary-range-nav"
                    style="display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1rem; max-width: 900px; margin: 0 auto;">
                    <!-- PREVIOUS -->
                    <a th:if="${prevSalaryUrl}" th:href="${prevSalaryUrl}" rel="follow" class="range-card prev-range"
                        style="display: flex; justify-content: space-between; align-items: center; padding: 1.5rem; background: white; border: 1px solid #ddd; border-radius: 12px; text-decoration: none; color: inherit; transition: all 0.2s ease;">
                        <div>
                            <span
                                style="display: block; font-size: 0.85rem; color: #666; text-transform: uppercase; letter-spacing: 0.5px;">Lower
                                Range</span>
                            <span style="display: block; font-size: 1.25rem; font-weight: 700; color: #333;"
                                th:text="${'$' + #numbers.formatInteger(salary - 10000, 0, 'COMMA')}">$90,000</span>
                        </div>
                        <span style="font-size: 1.5rem; color: #ccc;">&larr;</span>
                    </a>

                    <!-- CURRENT (Visual Only) -->
                    <div class="range-card current-range"
                        style="display: flex; justify-content: space-between; align-items: center; padding: 1.5rem; background: #f0fdf4; border: 2px solid #16a34a; border-radius: 12px;">
                        <div>
                            <span
                                style="display: block; font-size: 0.85rem; color: #15803d; text-transform: uppercase; letter-spacing: 0.5px; font-weight: 700;">Current
                                Analysis</span>
                            <span style="display: block; font-size: 1.25rem; font-weight: 800; color: #166534;"
                                th:text="${'$' + #numbers.formatInteger(salary, 0, 'COMMA')}">$100,000</span>
                        </div>
                        <span style="font-size: 1.5rem; color: #16a34a;">&bull;</span>
                    </div>

                    <!-- NEXT -->
                    <a th:href="${nextSalaryUrl}" rel="follow" class="range-card next-range"
                        style="display: flex; justify-content: space-between; align-items: center; padding: 1.5rem; background: white; border: 1px solid #ddd; border-radius: 12px; text-decoration: none; color: inherit; transition: all 0.2s ease;">
                        <div>
                            <span
                                style="display: block; font-size: 0.85rem; color: #666; text-transform: uppercase; letter-spacing: 0.5px;">Higher
                                Range</span>
                            <span style="display: block; font-size: 1.25rem; font-weight: 700; color: #333;"
                                th:text="${'$' + #numbers.formatInteger(salary + 10000, 0, 'COMMA')}">$110,000</span>
                        </div>
                        <span style="font-size: 1.5rem; color: #ccc;">&rarr;</span>
                    </a>
                </div>
            </div>
        </section>

        <!-- STATE-BASED CITY LINKS (Internal Linking Grid) -->
        <section class="related-cities-section" th:if="${relatedCities != null and !relatedCities.isEmpty()}">
            <div class="container">
                <h3>Compare with other cities in <span th:text="${city.state}">Texas</span></h3>
                <p>See how this salary compares in nearby cities:</p>
                <div class="city-links-grid">
                    <a th:each="relatedCity : ${relatedCities}"
                        th:href="@{/salary-check/{slug}/{amount}(slug=${relatedCity.slug}, amount=${salary})}"
                        rel="follow" class="city-link-card">
                        <span class="city-name" style="font-weight: 500;"
                            th:text="'Is ' + ${'$' + #numbers.formatInteger(salary, 0, 'COMMA')} + ' a good salary in ' + ${relatedCity.city} + '?'">
                            Is $100,000 a good salary in Dallas?
                        </span>
                    </a>
                </div>
            </div>
        </section>

        <!-- RETENTION & CROSS-LINK -->
        <section class="retention-section">
            <div class="container">
                <h2>Thinking of moving?</h2>
                <p>Compare this salary specifically against another city.</p>
                <a th:href="${compareUrl}" class="btn-primary">Compare with Popular Cities</a>
            </div>
        </section>

        <!-- Ad Slot: Bottom -->
        <div class="container">
            <div class="ad-container-bottom" id="ad-bottom-reality">
                <div class="ad-label">ADVERTISEMENT</div>
                <div class="ad-placeholder">
                    <span class="ad-hint">Relocation & Career Partner Slot</span>
                </div>
            </div>
        </div>

    </main>

    <div th:replace="~{fragments/footer :: footer}"></div>
</body>

</html>