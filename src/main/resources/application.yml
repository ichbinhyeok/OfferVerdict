spring:
  application:
    name: offer-verdict
  thymeleaf:
    cache: true

server:
  port: ${PORT:8080}

app:
  publicBaseUrl: ${APP_PUBLIC_BASE_URL:https://livingcostcheck.com}
  baselineLivingCost: 1800
  sitemapChunkSize: 5000
  devReloadEnabled: true

seo:
  salary-bucket:
    interval: 10000
    min: 30000
    max: 500000

logging:
  level:
    root: INFO
